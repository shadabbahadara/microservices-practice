spring:
  application:
    name: profile
  jpa:    hibernate:      ddl-auto: update
security:  oauth2:    resource:      token-info-uri: http://localhost:7171/oauth/check_token
    client:      client-id: mobile
      client-secret: pin